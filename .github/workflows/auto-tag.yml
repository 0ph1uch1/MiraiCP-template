name: auto tag

on:
  workflow_dispatch:
    inputs:
     v:
      required: true
      description: version tag
  push:
    branches: master
jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
    - name: Logging
      run: |
           echo "${{toJSON(github.event)}}"
